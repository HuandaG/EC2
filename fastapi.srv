[Unit]
Description=FastAPI app
After=network.target

[Service]
User=ec2-user
WorkingDirectory=/home/ec2-user/taller_ec2
ExecStart=/home/ec2-user/taller_ec2/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
